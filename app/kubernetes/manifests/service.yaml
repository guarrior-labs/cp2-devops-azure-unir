apiVersion: v1
kind: Service

metadata:
  name: cp2-game-service

spec:

  selector:
    app: cp2-game

  ports:

  - protocol: TCP

    port: 80

    targetPort: 5000

  type: LoadBalancer